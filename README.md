# 图片边框工具

一个**纯前端**的图片边框处理工具，用于给截图添加边框、阴影、圆角效果，适合微信公众号配图使用。

> **v2.0 重磅更新**：完全移除后端依赖，所有图片处理在浏览器中使用 Canvas API 完成，可直接部署到静态托管服务！

## 功能特性

- 📤 **图片上传**：支持拖拽或点击上传
- 🎨 **边框效果**：自定义颜色、宽度、样式
- ⭕ **圆角效果**：可调节圆角半径
- 🌑 **阴影效果**：模糊度、偏移量、颜色透明度
- 👁️ **实时预览**：所有效果即时预览
- 💾 **多格式导出**：PNG、JPG、WebP
- 🚀 **纯前端**：无需后端，直接在浏览器处理
- 📦 **静态部署**：可部署到任何静态托管服务

## 快速开始

### 开发模式

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

访问 http://localhost:5173

### 生产构建

```bash
# 构建静态文件
npm run build

# 预览构建结果
npm run preview
```

构建产物在 `frontend/dist` 目录，可直接部署到：
- Vercel
- Netlify
- GitHub Pages
- Cloudflare Pages
- 任何静态文件服务器

## 项目结构

```
image-border-tool/
├── frontend/            # 前端应用（Vue 3）
│   ├── src/
│   │   ├── components/  # Vue 组件
│   │   ├── composables/ # 组合式函数
│   │   ├── utils/       # 工具函数
│   │   │   ├── canvasProcessor.js  # Canvas 图片处理
│   │   │   └── colorExtractor.js   # 颜色提取
│   │   └── App.vue
│   └── dist/           # 构建输出（静态文件）
└── package.json
```

## 技术栈

- **前端框架**：Vue 3 + Composition API
- **构建工具**：Vite
- **样式**：TailwindCSS
- **图片处理**：原生 Canvas API（无需后端）
- **预览渲染**：Fabric.js

## 开发

### 核心文件

- `src/utils/canvasProcessor.js` - Canvas 图片处理核心逻辑
  - 边框绘制（支持圆角）
  - 阴影渲染
  - 圆角裁剪
  - 格式导出

- `src/composables/useImageEditor.js` - 状态管理
  - 图片加载
  - 效果参数管理
  - 导出控制

### 前端组件

- `ImageUploader.vue`：图片上传组件
- `PreviewCanvas.vue`：Fabric.js 预览画布
- `ControlPanel.vue`：参数控制面板

## 部署示例

### Vercel

```bash
npm run build
vercel --prod
```

### Netlify

```bash
npm run build
# 将 frontend/dist 目录连接到 Netlify
```

### Nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## License

MIT
